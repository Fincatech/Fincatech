environment = 'd';

config = Object();

if(environment == 'd')
{
    config.baseURLEndpoint = 'http://localhost/fincatech/api/v1/public/';
    console.log('URL: ' + config.baseURLEndpoint);
}

//  Rutas endpoint
